services:
  - type: web
    name: birthday-surprise-api
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: CORS_ORIGIN
        sync: false
      - key: FRIEND_NAME
        value: Bestie
