[build]
  base = "frontend"
  command = "npm run build"
  publish = "out"

# Static export — do NOT use @netlify/plugin-nextjs or publish ".next"

[build.environment]
  NODE_VERSION = "20"
  NETLIFY = "true"
  NETLIFY_NEXT_PLUGIN_SKIP = "true"